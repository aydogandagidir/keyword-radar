# Task 00 — Bootstrap Monorepo

Use the master prompt as context.

Create the monorepo structure for Bluedev Marketplace Keyword Radar.

Requirements:
- Use pnpm workspace.
- Create `apps/extension`, `apps/web`, and `packages/*`.
- Add root `package.json`.
- Add `pnpm-workspace.yaml`.
- Add `tsconfig.base.json`.
- Add shared lint/build/test scripts.
- Add Vitest setup.
- Add README with development commands.

Acceptance criteria:
- `pnpm install` works.
- `pnpm build` runs.
- `pnpm test` runs.
- Folder structure matches the architecture.
